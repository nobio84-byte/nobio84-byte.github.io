name: Register Tesla
on:
  workflow_dispatch:
jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 register_tesla.py
        env:
          TESLA_CLIENT_SECRET: ${{ secrets.TESLA_CLIENT_SECRET }}
